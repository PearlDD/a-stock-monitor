---
tags:
  - factory
  - strategy
  - spec
date: 2026-07-14
source: factory-archivist
---

# Strategy: spec — 2026-07-14 (Revised)

## CEO Verdict: PROCEED

2 hypotheses approved. Well-scoped with explicit growth dimension tags. Good dependency ordering: H1 scaffold → H2 data layer. No issues found.

## Current Score: 0.413

### Weakest Dimensions
- observability: 0.0
- capability_surface: 0.04
- research_grounding: 0.32

## Approved Hypotheses

### H1: Project Scaffold (Priority: HIGH)
- **Category:** EXPLORE
- **Growth dimensions:** capability_surface, observability
- **Scope:** pyproject.toml, src/app/ package (main.py app factory, config.py Pydantic Settings, models/market.py dataclasses, providers/base.py ABC, providers/mock.py MockProvider, logging.py structlog), .env.example, CLAUDE.md, tests (conftest.py, test_smoke.py, test_providers.py)
- **Key requirements:** MockProvider with set_price()/inject_news(), structlog from day 1, smoke test must pass
- **Expected impact:** capability_surface 0.04→0.15, observability 0.0→0.3, tests 0.5→0.8, lint 0.5→0.8

### H2: Data Collection Layer (Priority: HIGH)
- **Category:** EXPLORE
- **Growth dimension:** capability_surface
- **Scope:** AKShareTHSProvider (asyncio.to_thread wrapping, THS interfaces), rate_limiter.py (asyncio.Lock, 3s interval, exponential backoff), database.py (aiosqlite, WAL mode, 5-table schema, PRAGMA tuning), tests (test_rate_limiter.py, test_database.py, test_akshare_provider.py)
- **Key requirements:** asyncio.to_thread() for all sync AKShare calls, WAL mode + busy_timeout=5000, no network calls in tests
- **Expected impact:** capability_surface 0.15→0.35, observability 0.3→0.5

## Anti-patterns (from research)
- No East Money endpoints (broken since Feb 2026, akfamily/akshare#7051)
- No sync AKShare in async context — must use asyncio.to_thread()
- No skipping rate limiting — opaque anti-crawling
- No standard sqlite3 — must use aiosqlite
- No hardcoded tokens/keys

## New Backlog Items (5)
1. Monitoring engine: alert evaluation, cooldown dedup, trading hours gate, APScheduler
2. PushPlus client: POST to pushplus.plus/send, 180/day DB tracking, --test CLI mode
3. REST API: stock watchlist CRUD, alert rule CRUD, push history, test-push endpoint
4. Web UI: HTMX + Jinja2 dashboard, SSE real-time, watchlist/alert/history views
5. Docker: multi-stage Dockerfile, docker-compose, /health, daily heartbeat

## Builder Instructions
- H1: Create pyproject.toml, src/app/ package, MockProvider, structlog, tests. Smoke test must pass.
- H2: AKShare THS provider with asyncio.to_thread(), rate limiter, aiosqlite DB layer, tests.
